# Supabase (required for online leaderboard). Copy to .env and fill in.
# Get these from Supabase Dashboard → Project Settings → API.
# The anon key is safe in the browser when RLS is enabled; we still use .env so
# you can rotate keys and avoid committing values.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
